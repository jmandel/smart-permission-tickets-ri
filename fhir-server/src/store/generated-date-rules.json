{
  "_comment": "Designated date elements per the spec's Data Period Enforcement table. One designated element per resource type, no fallbacks: a resource with no value in its designated element does not match a data_period window. Resource types absent here and not identity-exempt are excluded entirely when data_period is present.",
  "default": {
    "allowEncounterFallback": false,
    "rules": []
  },
  "resourceTypes": {
    "AllergyIntolerance": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "AllergyIntolerance.recordedDate",
          "kind": "instant",
          "startPaths": ["recordedDate"]
        }
      ]
    },
    "Condition": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "Condition.recordedDate",
          "kind": "instant",
          "startPaths": ["recordedDate"]
        }
      ]
    },
    "DiagnosticReport": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "DiagnosticReport.effectivePeriod",
          "kind": "period",
          "startPaths": ["effectivePeriod.start"],
          "endPaths": ["effectivePeriod.end"]
        },
        {
          "label": "DiagnosticReport.effectiveDateTime",
          "kind": "instant",
          "startPaths": ["effectiveDateTime"]
        }
      ]
    },
    "DocumentReference": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "DocumentReference.date",
          "kind": "instant",
          "startPaths": ["date"]
        }
      ]
    },
    "Encounter": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "Encounter.period",
          "kind": "period",
          "startPaths": ["period.start"],
          "endPaths": ["period.end"]
        }
      ]
    },
    "Immunization": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "Immunization.occurrenceDateTime",
          "kind": "instant",
          "startPaths": ["occurrenceDateTime"]
        }
      ]
    },
    "MedicationRequest": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "MedicationRequest.authoredOn",
          "kind": "instant",
          "startPaths": ["authoredOn"]
        }
      ]
    },
    "Observation": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "Observation.effectivePeriod",
          "kind": "period",
          "startPaths": ["effectivePeriod.start"],
          "endPaths": ["effectivePeriod.end"]
        },
        {
          "label": "Observation.effectiveDateTime",
          "kind": "instant",
          "startPaths": ["effectiveDateTime"]
        }
      ]
    },
    "Procedure": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "Procedure.performedPeriod",
          "kind": "period",
          "startPaths": ["performedPeriod.start"],
          "endPaths": ["performedPeriod.end"]
        },
        {
          "label": "Procedure.performedDateTime",
          "kind": "instant",
          "startPaths": ["performedDateTime"]
        }
      ]
    },
    "ServiceRequest": {
      "allowEncounterFallback": false,
      "rules": [
        {
          "label": "ServiceRequest.authoredOn",
          "kind": "instant",
          "startPaths": ["authoredOn"]
        }
      ]
    }
  }
}
